package Data;import IntentRecognizer.IntentRecognizer;/** * 消息类 * 用于表示一次对话中的消息，包含发送者、内容、附加信息和时间戳等信息 */public class Message {    /**     * 消息发送者，可以是"user"或"bot"     */    private final String sender;    //对象，user或bot        /**     * 消息内容，即聊天的具体文本内容     */    private final String content;   //聊天内容        /**     * 消息附加信息，如NLP工具的分析记录     */    private final IntentRecognizer.IntentAnalysisResult addition;  //备注，如NLP工具的分析记录        /**     * 消息时间戳，记录消息创建的时间     */    private final long timestamp;   //时间戳    /**     * 构造函数，创建一个不包含附加信息的消息     *      * @param sender 消息发送者("user"或"bot")     * @param content 消息内容     */    public Message(String sender, String content) {        this.sender = sender;        this.content = content;        this.addition = null;        this.timestamp = System.currentTimeMillis();    }    /**     * 构造函数，创建一个包含附加信息的消息     *      * @param sender 消息发送者("user"或"bot")     * @param content 消息内容     * @param addition 消息附加信息，如NLP分析结果     */    public Message(String sender, String content, IntentRecognizer.IntentAnalysisResult addition) {        this.sender = sender;        this.content = content;        this.addition = addition;        this.timestamp = System.currentTimeMillis();    }    /**     * 获取消息发送者     *      * @return 发送者标识("user"或"bot")     */    public String getSender() {        return sender;    }    /**     * 获取消息内容     *      * @return 消息文本内容     */    public String getContent() {        return content;    }    /**     * 获取消息附加信息     *      * @return 消息的附加信息，如NLP分析结果     */    public  IntentRecognizer.IntentAnalysisResult getAddition() {        return addition;    }    /**     * 获取消息时间戳     *      * @return 消息创建时的时间戳(毫秒)     */    public long getTimestamp() {        return timestamp;    }    /**     * 返回消息的字符串表示形式     *      * @return 格式化的消息字符串，包含发送者、内容和附加信息(如果有)     */    @Override    public String toString() {        if(addition == null) {            return String.format("%s: %s", sender, content);        }else {            return String.format("%s: %s\n%s", sender, content, addition.toString());        }    }}